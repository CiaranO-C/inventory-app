<%- include('partials/head') %>
<div class="overlay">
  <dialog class="modal" open>
    <form action="/dashboard/items/<%= itemId %>" method="POST">
      <label for="password">Password:</label>
      <input type="password" name="password" id="password" required />
      <% if(locals.errors) { %>
        <ul>
        <% errors.forEach(err => { %>
            <li><%= err.msg %></li>
            <% }) %>
        </ul>
        <% } %>
      <a href="/dashboard/items/<%= itemId %>">Go back</a>
      <button type="submit">Submit</button>
    </form>
  </dialog>
</div>
